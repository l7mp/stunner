name: Tests

on:
  workflow_call:
  workflow_dispatch:
  push:
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
    branches:
      - '*'
    tags-ignore:
      - '*'
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26'
      - name: Download modules
        run: go mod download
      - name: Go install
        run: go install
      - name: Test
        run: go test -v -covermode=count -coverprofile=coverage.out
      - name: Coveralls
        uses: coverallsapp/github-action@v2
        if: github.repository == 'l7mp/stunner'
        with:
          github-token: ${{ secrets.github_token }}
          file: coverage.out
          format: golang
  seccheck:
    name: Security Check
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v6
      - name: Check security vulnerabilites
        id: scan
        uses: anchore/scan-action@v7
        with:
          path: "."
          fail-build: true
          severity-cutoff: critical
          output-format: table
          output-file: grype-results.txt
      - name: Surface vulnerability report
        if: always()
        run: |
          echo "::group::Grype vulnerability report"
          cat grype-results.txt || echo "no grype report was produced"
          echo "::endgroup::"
